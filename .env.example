# Optional: reranking with OpenAI or Gemini; leave empty to use local cross-encoder
OPENAI_API_KEY=
GEMINI_API_KEY=
# Model names (optional overrides)
EMBED_MODEL=sentence-transformers/all-MiniLM-L6-v2
CROSS_ENCODER=cross-encoder/ms-marco-MiniLM-L-6-v2
# Paths (override if desired)
FAISS_PATH=data/index.faiss
META_PATH=data/meta.parquet
